Archived standalone root scripts moved out of the active repository surface.

- These files are not part of the current documented entrypoints or CI flow.
- They are retained only for historical research and debugging reference.
- New active scripts should live under the current `scripts/` hierarchy or `tests/`.
